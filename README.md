# Request Header Parser Microservice

This is a solution to the FreeCodeCamp Request Header Parser Microservice project.

## Features

- Returns your IP address
- Returns your preferred language
- Returns your software information

## Usage

Make a GET request to `/api/whoami`:

```bash
curl http://localhost:3000/api/whoami
